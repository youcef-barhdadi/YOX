using System;
using System.Collections.Generic;
using System.Text;
using System.IO;

namespace Lox
{
    public class Helper
    {
        //public static defineAst(string dir, string className, string superclass, List<string> list)
        //{
        //    string path = dir + '/' + className + ".cs";
        //    Stream s = File.Create(path);
        //    StreamWriter stream = new StreamWriter(s);
        //    stream.WriteLine("using System;");
        //    stream.WriteLine("using System.Collections.Generic;");
        //    stream.WriteLine("namespace Lox {");
        //    stream.WriteLine($"   public  class  {className} : {superclass} {{ ");
        //    stream.WriteLine(" }");
        //    stream.Close();
        //}
    }
}
